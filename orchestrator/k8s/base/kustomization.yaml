apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - service-account.yaml
  - ingress.yaml

commonLabels:
  app: orchestrator
  managed-by: kustomize
